import React from 'react';

import { Container, Title, Button, ButtonText } from './styles';

const Header = () => {
  return (
    <Container>
      <Title>Header</Title>
      <Button>
        <ButtonText>OPÇÔES</ButtonText>
      </Button>
    </Container>
  );
};

export default Header;
